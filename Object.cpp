#include "Object.h"

void Object::Initialize() {
    //uint32_t a = TextureManager::Load(".png");

    //uint32_t b = TextureManager::Load(".png");

    //uint32_t c = TextureManager::Load(".png");

    //uint32_t d = TextureManager::Load(".png");

    // a = Sprite::Create(Title, {640, 360}, {1, 1, 1, 1}, {(0.5f), (0.5f)});

    // b = Sprite::Create(menu, {640, 360}, {1, 1, 1, 1}, {(0.5f), (0.5f)});

    // c = Sprite::Create(clear, {640, 360}, {1, 1, 1, 1}, {(0.5f), (0.5f)});

    // d = Sprite::Create(over, {640, 360}, {1, 1, 1, 1}, {(0.5f), (0.5f)});

    objectPos={1,1};
}

void Object::Update() {}

void Object::Draw() {}
